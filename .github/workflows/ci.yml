diff --git a/.codex_config.yaml b/.codex_config.yaml
index e6fb9c159d89b5cdc444804191c1670fc6911d53..941be538d1be384ea7559433268a41a96a2d51ec 100644
--- a/.codex_config.yaml
+++ b/.codex_config.yaml
@@ -1,5 +1,28 @@
 rules:
   code_change_triggers_tests: true
   doc_changes_skip_tests: true
+  branch_pattern: "codex/{feature-name}"
+  full_build_triggers:
+    - core
+    - engine
+    - matrix
+    - protocol
+    - epoch
+    - echelon
+    - hotfix
+    - merge
   commit_message_format: "[type] message"
   enforce_manifest: true
+  prohibit_eval_exec: true
+  no_placeholders: true
+  required_folders:
+    - core
+    - modules
+    - gui
+    - cli
+    - config
+    - docs
+    - tests
+  output_paths:
+    zip: "output/ZIP/"
+    docx: "output/docx/"
